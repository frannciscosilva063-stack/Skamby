{
  "name": "skamby-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @skamby/api --filter @skamby/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  }
}
